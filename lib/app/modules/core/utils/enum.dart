enum EText { body, error, primary, white, subtitle }

enum EDevice {
  mobile,
  tablet,
  desktop,
}

enum ERequestMethod {
  get,
  post,
  patch,
  delete,
}

enum ESize {
  sm,
  md,
  lg,
}

enum EAsyncDropdown { tyep1 }
