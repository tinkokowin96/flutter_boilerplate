enum ETextType { body, error, primary, white, subtitle }

enum EText { h1, h2, h3, h4, h5 }

enum EDevice {
  mobile,
  tablet,
  desktop,
}

enum ERequestMethod {
  get,
  post,
  patch,
  delete,
}

enum ESize {
  sm,
  md,
  lg,
}

enum EAsyncDropdown { tyep1 }
